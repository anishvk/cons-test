platform: linux

image_resource:
  type: registry-image
  source:
    repository: openjdk
    tag: 11-jdk

inputs:
- name: app-source-code

run:
  path: sh
  args:
  - -exc
  - |
    cd app-source-code
    ./gradlew test
